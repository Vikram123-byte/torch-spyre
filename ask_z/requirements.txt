# ask_z/requirements.txt
# ──────────────────────
# Dependencies for the Ask-Z service (separate from torch_spyre core).
# Install with:  pip install -r ask_z/requirements.txt

# ── Elasticsearch 8.x client ──────────────────────────────────────────────────
elasticsearch>=8.13.0,<9.0.0

# ── Ingestion & RAG pipeline ──────────────────────────────────────────────────
# Pinned to 0.10.x — 0.11+ requires Python ≥ 3.10 (banks dependency uses X|Y union syntax)
llama-index-core>=0.10.0,<0.11.0
llama-index-readers-github>=0.1.0
llama-index-vector-stores-elasticsearch>=0.2.0

# ── IBM watsonx.ai SDK ────────────────────────────────────────────────────────
# Primary embedding:  ibm/granite-embedding-278m-multilingual (768 dims)
# Primary reranker:   ibm/slate-125m-english-rtrvr-v2 (native rerank on watsonx.ai)
# Endpoint confirmed: https://us-south.ml.cloud.ibm.com/ml/v1/text/embeddings
ibm-watsonx-ai>=1.1.0,<2.0.0

# ── Embeddings local fallback (on-prem / offline) ────────────────────────────
# Used when WATSONX_API_KEY is not set.
# BAAI/bge-large-en-v1.5 outputs 1024 dims — if using locally, set
# EMBEDDING_MODEL=BAAI/bge-large-en-v1.5 and VECTOR_DIMS env override.
sentence-transformers>=3.0.0,<4.0.0

# ── API service ───────────────────────────────────────────────────────────────
fastapi>=0.111.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
httpx>=0.27.0,<1.0.0              # async HTTP client (Bob API + IAM token exchange)

# ── Utilities ─────────────────────────────────────────────────────────────────
pydantic>=2.7.0,<3.0.0
python-dotenv>=1.0.0              # load .env in local dev
tenacity>=8.3.0                   # retry logic for ES / watsonx.ai / Bob API calls
regex>=2024.0.0                   # required by project lint rule (import regex as re)

# ── Document parsing (PDF / PPTX / DOCX for external-doc ingestion) ──────────
pymupdf>=1.24.0                   # PDF extraction (fitz)
python-pptx>=0.6.23               # PowerPoint extraction
python-docx>=1.1.0                # Word document extraction
